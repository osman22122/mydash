apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns
  namespace: {{ .Values.namespace }}
  labels:
    grafana_dashboard: "1"
data:
    k8s-coredns.json: |-
{{ .Files.Get "dashboards/k8s-coredns.json" | indent 4 }}
